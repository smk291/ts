export type Sides = number[];

export type Coordinates = {
  point0: [number, number];
  point1: [number, number];
  point2: [number, number];
};

export type LabeledSides = {
  a: number,
  b: number,
  c: number,
};

